# 2026-07-24

## 构建与部署
- Tauri Windows 构建成功：`app/src-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis/SkyXing_2.0.1_x64-setup.exe`
- Web 端 (`web/`) `npm run build` 成功，输出到 `public/`；构建后还原了 `terms.html`/`privacy.html`（vite emptyOutDir 会清空 public/）
- Cloudflare 部署成功：`wrangler deploy` → Version `c2193b87`，生产验证通过（`/server/api/config` 正常返回，`/server/api/feedback/list` 返回 auth required 证明路由已注册）
- Git 提交 `67e63ab` 并推送到 `origin/master`
- (下午) Git 提交 `d882ef9` 推送到 `origin/master`，Cloudflare 部署 Version `3b3ccf56`（`wrangler deploy` 不用代理，OAuth 凭证 1470485062@qq.com）

## v2.0.1-beta.3 实现（2026-07-24 后半段）
### 版本号
- `app/package.json`, `app/src-tauri/tauri.conf.json`, `app/src-tauri/Cargo.toml` → `2.0.1-beta.3`

### 任务1：翻译功能（敬请期待 UI）
- i18n 字典（app + web）新增 `settings.translation*` 系列键（zh/en）
- App/Web/Windows 三端设置页「个性化 → 语言」下新增「翻译」L4 叶子屏幕，含：
  - 启用/禁用开关（灰色禁用，`disabled` 属性）
  - 显示模式 Seg（纯译文/双语对照，灰色不可操作）
  - 服务测试按钮（模拟测试，提示「敬请期待」）
  - 底部黄色敬请期待提示条
- 后端翻译路由待后续实现

### 任务2：Web 设置迁移到 App + 设置层级重调整
- 三端设置树重新调整（2026-07-24 下午）：
  ```
  设置 → 个性化 → 外观(L3→L4: 动画/主题)
                   → 语言(L3→L4: 语言/翻译)
                   → 状态栏(叶子)
         → 账号数据 → 数据同步(叶子) / 账号管理(L3→L4: 2FA/注销)
         → 反馈 → GitHub反馈 / 提交反馈
         → 软件 → 更新(L3→L4: 渠道/下载源/检查更新)
                  → 调试(L3→L4: 日志/终端/调试信息)
                  → 关于软件(L3→L4: 重置/关于后端/关于软件/关于团队)
  ```

### 任务3：安卓状态栏修复
- `app/src/context/SettingsContext.jsx` 新增 `statusBarEnabled`(默认 true) + `statusBarHeight`(默认 28px)
- `app/src/index.css` 新增 `:root { --status-bar-padding: 28px; }` + `.status-bar-padding` 类
- `app/index.html` 首屏脚本同步 `--status-bar-padding` 变量
- App/Web/Windows 三端设置页「个性化」下新增「状态栏」叶子屏幕，含：
  - 启用/禁用开关（实时更新 CSS 变量）
  - 高度滑块（0-120px，步进 1px，显示当前值）
- Windows `SettingsContext` 同步新增 `statusBarEnabled`/`statusBarHeight` 默认值

### 任务4：首页/博客拆分 + 系统通知弹窗（2026-07-24 下午新增）
- **App 端**：HomePage 重写为首页（搜索/快捷入口/用户信息/系统通知弹窗）；新增 BlogPage（原 PodcastPage 迁移，文章卡片网格）；路由 `/blog` + 旧 `/podcast` 兼容
- **Web 端**：HomePage 重写为首页（去掉文章列表，加快捷入口/系统通知弹窗）；BlogPage 重写（纯文章列表+标签筛选+置顶推荐）
- **系统通知**：App/Web HomePage 加载后自动调用 `GET /notifications?systemOnly=true`，有未读系统通知则弹出模态弹窗（仅弹1次，用 localStorage 标记）
- **后端**：`src/routes/notifications.js` 新增 `systemOnly` 查询参数支持，只返回 `category === 'system'` 的通知

### 任务5：L4 介绍屏幕（2026-07-24 晚）
- 每个 L4 菜单项现在有独立的功能介绍页面，而非直接跳转到旧版设置屏
- 三端统一新增 L4 介绍组件：
  - 更新→渠道：介绍渠道含义 + 渠道选择器
  - 更新→下载源：介绍下载源区别 + 源选择器
  - 更新→检查更新：介绍 + 检查更新按钮
  - 调试→日志记录：介绍日志功能
  - 调试→终端：介绍终端功能
  - 调试→调试信息：介绍 + 诊断信息显示
  - 关于→重置：介绍 + 重置按钮
  - 关于→关于后端：介绍后端架构
  - 关于→关于团队：介绍团队
  - 关于→关于软件：仍指向旧版 AboutScreen（含实际功能）
- Web 构建修复：SyncScreen 缺少函数声明问题已修正
