# 2026-07-21

## Task C（三端设置三级/四级层级 + 反馈 + 条款同步）— 代码改动（未构建/未部署）

延续前一轮（版本号 2.0.1、AdminPage 重写、Android SlideOutlet 安全区已落地）。

本次完成：
- **后端反馈路由** `src/routes/feedback.js`：`POST /feedback/submit`（authOptional，存 KV `feedback:`）、`GET /list`、`DELETE /:id`（均 adminRequired）。`src/utils/kv.js` 新增 `PREFIX.FEEDBACK`；`src/index.js` 注册 `api.route('/feedback', feedbackRoutes)`。
- **API 客户端**：`app/src/lib/api.js` 与 `web/src/lib/api.js` 各加 `submitFeedback()` / `getFeedbackList()`。
- **updateSource（github/ghfast）**：App `SettingsContext` 与 Windows `SettingsContext`（DEFAULTS）新增 `updateSource`（默认 'github'）；App 端经 `useSettings` 暴露。
- **i18n**：app + web 的 `lib/i18n.js` 各补充设置层级/反馈/更新源等键（zh+en）。
- **三端设置页重写为可导航层级**：
  - `app/src/pages/SettingsPage.jsx`（主端，--win-* token）
  - `web/src/pages/SettingsPage.jsx`（Tailwind）
  - `clients/windows/src/components/SettingsModal.jsx`（含内部导航栈，叶子屏幕自读 context 以保状态实时）
  - 层级：设置 → {个性化, 账号数据, 反馈, 软件} → L3 项；含>1 子项者展开 L4（外观→语言/动画/主题；账号管理→2FA/注销）。新增「软件-更新」承载渠道/源切换。
  - 二次确认（ConfirmDialog）用于：注销账号、关闭 2FA、调试开关。
  - 更新下载按 `updateSource` 选 `proxyUrl`(ghfast) 或 `url`(github)。
- **条款同步**：`public/` 与 `web/public/` 的 `terms.html`、`privacy.html` 重写为类 B 站许可证结构（十章），保留账号归属与非官方免责声明，更新日期 2026-07-21 / v2.0.1。

注意：用户明确「本次不构建」，故仅改代码未 build/deploy；下次需构建验证三端 + `wrangler deploy` 后端。
