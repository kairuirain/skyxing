# 2026-07-20 工作记录

## SkyXing 需求实现（13 项需求，先后端 → Web → App 测试）

### 决策（用户确认）
- 人机验证：Cloudflare Turnstile（免费、全球含中国大陆可用）。
- 数据同步：版本号轮询（CF Workers 不支持持久 WebSocket）。
- 优先级：先后端，再 Web，最后 App 测试。

### 后端（src/）新增/修改
- 新增 `utils/turnstile.js`（Turnstile 校验，未配密钥时开发放行）、`utils/ratelimit.js`（KV 滑动窗口计数器）、`utils/sync.js`（同步版本号+设置提取）、`middleware/cache.js`、`routes/verify.js`（/verify/turnstile、/verify/bot-status）、`routes/sync.js`（GET/PUT /sync、/sync/version）、`routes/bootstrap.js`（首屏批量接口）。
- `index.js`：注册新路由；`/server/api/health` 扩展为检测 KV/缓存/队列/API 微服务；新增 `/health` 可视化状态页；新增 `/server/api/config`（返回 turnstileSiteKey、clientMinVersion）。
- `routes/auth.js`：注册写入 agreedToTerms/language/animationMode/debugEnabled；登录与注册加入防人机验证（登录失败≥5 次/IP 注册≥5 次/小时强制 Turnstile）。
- `routes/notifications.js`：标记已读时 bump 同步版本号（跨端通知一致）。
- `routes/users.js`：修复严重 bug——个人资料更新原用 JWT 载荷覆盖完整用户对象，会丢失 passwordHash/totpSecret/设置字段；改为基于完整对象更新，并 bump 同步版本号。
- `kv.js`：新增 SYNC、RATELIMIT 前缀。

### Web（web/src/）新增/修改
- 新增 `lib/i18n.js`（中/英词典 + detectOSLanguage）、`context/I18nContext.jsx`、`context/AnimationContext.jsx`（动画三模式+调试开关，data-anim 控制 CSS）、`context/SyncContext.jsx`（登录拉取+15s 轮询+冲突以后端为准）、`components/TurnstileWidget.jsx`。
- `main.jsx`：挂载 I18n/Animation/Sync Provider。
- `lib/api.js`：新增 sync/verify/bootstrap/config 方法；错误携带 data（识别 needTurnstile）。
- `pages/SettingsPage.jsx`：语言/动画/主题切换、调试开关、同步按钮、服务状态检测（开 /health）、2FA 模态窗口。
- `pages/LoginPage.jsx` / `RegisterPage.jsx`：接入 i18n + Turnstile（按需渲染）。
- `components/Layout.jsx`：导航标签 i18n 化。
- `index.css`：[data-anim="minimal"] 关闭动画、[data-anim="rich"] 增强动效。
- 构建通过（vite build ok）。

### 共用 App API（clients/shared/api.js）
- 补充 2FA、Notifications、sync、verify、bootstrap、config 方法；request 错误携带 data。Windows/Android 两端可直接调用。

### 部署注意
- 后端需 `wrangler secret put TURNSTILE_SECRET_KEY`；站点密钥经 `TURNSTILE_SITE_KEY` 环境变量暴露给前端（/config 返回）。未配置时 Turnstile 自动放行（开发友好）。

### 后端本地验证（wrangler dev --local，端口 8792）
- 全部新端点验证通过：`/server/api/config`、`/server/api/health`(status=ok, KV 探测成功)、`/server/api/verify/bot-status`、`/server/api/bootstrap`(返回 2 篇已发布文章)、注册(201, 写入 language/animationMode/agreedToTerms)、登录(200)、`GET /sync`(设置回读正确)、`PUT /sync`(版本号 0→1 bump 正确)、`/server/api/lookup/SkyXing`(200, role=official)。
- 修复 `src/index.js` `/lookup/:username`：读取用户名索引漏传 `false` 导致 404（kvGet 默认 parseJson 对原始字符串 id 解析失败返回 null）。
- 修复 `ensureOfficialAccount`：幂等判断读取索引改用 `false`，并对"已存在但角色非 official"的旧数据进行修复（兼容本地旧 KV）。
- 重建空文件 `src/utils/turnstile.js`、`src/routes/verify.js`（此前 write_to_file 落盘为空导致 worker 启动失败），已通过 `wrangler deploy --dry-run` 确认无 "no exports" 警告。
- 临时验证脚本（probe.mjs/fulltest.mjs）与 dev*.log 已清理。

### 提交 web 部署
- `git commit`（未推送）于 `master` -> `b19ce1e`（35 files, +2338/-1095）。
- 暂存范围：Worker 源码 `src/`、前端 `web/`、构建产物 `public/`(新 hash index-CWJRmhls.js / index-DokFjbmX.css，删除旧 BStWG-c_/DNkmK4Kf)、`wrangler.toml`、根 `package.json/package-lock.json`、`.github/workflows/build-and-release.yml`(多端发布 CI)。
- 显式排除（未提交）：`clients/`(App 端 API)、`.dev.vars`(本地 JWT 密钥)、`.codebuddy/`(记忆)、`.wrangler-build/`、`.tools/`(WASM 构建工具)、以及 be.err/be.out/check.js/dev.err/dev.out/dl.js/MIGRATION_TO_NATIVE.md 等 scratch 日志。原因：`.gitignore` 未忽略这些文件，`git add -A` 会泄漏密钥与记忆，故采用精准路径暂存。

### 推送与部署
- `git push origin master` 成功：`ef80c9a..b19ce1e  master -> master`（远程 github.com/kairuirain/skyxing.git）。
- `wrangler deploy` 成功：Uploaded/Deployed，URL `https://skyxing.1470485062.workers.dev`，Version ID `2a214750-d416-4fda-b80b-6cc8753bf678`。静态资源读取 6 个文件，"No updated asset files to upload"（资源 hash 未变）。
- 沙箱无外网出口，无法从本地 curl/fetch 验证生产端点（fetch failed）；部署成功以 wrangler 自身回执为准。
- 注意：生产环境 `JWT_SECRET`/`TURNSTILE_SECRET_KEY` 需经 `wrangler secret put` 预先配置；若未配，worker 会回退到不安全兜底密钥（仅本地开发用）。上线后应在 Cloudflare 后台确认这两项 secret 已设置。

### 生产 Turnstile 配置完成
- `wrangler secret put TURNSTILE_SECRET_KEY` 成功（secret）。
- 站点密钥（公开）经 `wrangler.toml` 的 `[vars] TURNSTILE_SITE_KEY` 配置（wrangler v4 无 `variable put` CLI，明文变量须写在 toml）；已提交并推送 `e60a0db`。
- 重新部署 Version `a47f9e51-9687-4962-b3b0-f3130593ffc8`，绑定 env.TURNSTILE_SITE_KEY + secret TURNSTILE_SECRET_KEY，生产 Turnstile 已启用。
- Turnstile 小组件：托管(Managed) 模式，主机名 `skyxing.dpdns.org`（项目实际访问域名，非 workers.dev）。预清除(Pre-Clearance) 未开启（站点未走 Cloudflare 代理、且无 Cloudflare 安全规则可绕过）。
- 沙箱无外网，无法本地验证 `/verify/bot-status`（fetch failed）；以 wrangler 部署回执中绑定列表确认生效。
- 用户侧浏览器实测 `GET /server/api/verify/bot-status` 返回 `{"turnstileEnabled":true,"siteKey":"0x4AAAAAAD5wIJ0zrusozG2N"}` → 生产 Turnstile 已确认生效。

### 登录/注册人机验证触发策略
- 用户最终决定：**登录与注册均“始终要求”Turnstile**（非阈值风控）。
- 后端 `src/routes/auth.js`：注册与登录均去掉阈值 gate，改为 `verifyTurnstile` 始终校验；未配密钥（本地）自动放行。移除了不再使用的 `getCounter` 导入（保留 incr/resetCounter 计次）。
- 前端 `RegisterPage.jsx`/`LoginPage.jsx`：`getConfig` 拿到 siteKey 即 `setNeedTurnstile(true)`，验证组件预先渲染，提交始终带 token。
- 提交并部署：`675bdb4`，生产版本 `c301a052-da0f-472d-968e-e0e586d0886e`，静态资源已更新（index-DVgVdtYT.js）。

### 修复 TurnstileWidget 无限重验证循环
- 现象：登录/注册页 Turnstile“一直检验我是否为真人”。控制台大量 `Blocked script execution in 'about:blank'`（挑战 iframe 反复重建）。
- 根因：`TurnstileWidget` 的 `useEffect` 依赖含 `onVerify/onError`，而父组件传内联箭头函数 → 每次父 re-render 生成新函数引用 → effect 重跑 → widget 反复卸载/重建 → 无限重新验证。
- 修复：`web/src/components/TurnstileWidget.jsx` 将回调存进 ref，effect 仅依赖 `[siteKey]`。
- 部署：`e80cf6d`，版本 `2e814a7a-6ed5-4456-b46d-4dfeaff8dc55`。
- 说明：控制台中的 bitwarden/扩展报错、Turnstile postMessage origin 提示、WOFF2 OTS 解析告警均为无关/良性，无需处理。

### 纠偏：Windows 客户端真正工程是 Tauri 的 `app/`（非 Electron 的 clients/windows）
- **重要**：`clients/windows` 是旧 Electron 客户端（已弃用方向）；真正在用的是 `app/`（Tauri v2 工程，同时产出 Windows .exe 与 Android .apk）。此前误在 `clients/windows` 用 electron-builder 构建，触发 electron 二进制下载（113MB）。用户指示"用 tauri"并删掉误下载物 → 已删除 `%LOCALAPPDATA%\electron` 半成品缓存。
- `app/` 关键结构：前端 React19+Vite6（端口 1420），版本经 `vite.config.js` 的 `define __APP_VERSION__ = pkg.version` 注入；`src-tauri/tauri.conf.json`（version 驱动安装包名）、`src-tauri/Cargo.toml`（crate version）。构建脚本 `npm run tauri:build:windows`（= `tauri build --target x86_64-pc-windows-msvc`）。
- 版本改动需同步三处：`app/package.json`、`app/src-tauri/tauri.conf.json`、`app/src-tauri/Cargo.toml`。semver 预发布标识 `2.0.1-beta.1` 合法（无前导零）。
- 环境：Rust 1.97.0，已装 `x86_64-pc-windows-msvc` 目标 + 安卓多目标 + wasm32；MSVC 工具链由 cargo 自动定位（link/cl 不在 PATH 属正常）。

### Windows 端 2.0.1-beta.1 Tauri 构建成功
- 三处版本号已改为 `2.0.1-beta.1`；`npm run tauri:build:windows` 通过：vite build ok（1641 模块），Rust release 编译 3m10s，NSIS 打包成功。
- 产物：`app/src-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis/SkyXing_2.0.1-beta.1_x64-setup.exe`（3,879,046 字节），已按惯例复制归档到 `app/releases/Windows/`。
- 安装包命名惯例：`SkyXing_<version>_x64-setup.exe`；releases 目录 Windows/ 与 Android/ 分别存 .exe 和 .apk。
- 注意：`clients/windows/package.json` 与 `SettingsModal.jsx` 此前被误改为 2.0.1-beta.1（那是旧 Electron 客户端），且我在其中误装了 @tauri-apps/cli/lucide-react/terser——如需彻底清理旧 Electron 客户端可后续处理，本次未动。

### 用户反馈：app 端功能缺 + 联网全失败（"你做错了"）
- 用户实测 2.0.1-beta.1：所有 API 请求失败 `error sending request for url`（连接层），导致无法登录；且 web 端有但 app 端缺的功能包括：**登录/注册的人机验证 Turnstile、多语言 i18n、主题、动画模式、跨端同步**，以及"所有内容"。
- 诊断结论：① 后端 `https://skyxing.dpdns.org` **在线**（WebFetch 实测 `/state/version` 正常返回）；② capabilities 已正确允许 `https://skyxing.dpdns.org/**`（http:allow-fetch）；③ 用户网络**不需要代理**；④ `app/src/pages/LoginPage.jsx` 确实**无 Turnstile 组件**（web 有）；⑤ app 端 context 仅 Auth/Settings/Transition，缺 I18n/Sync/Animation/Theme（与 MEMORY.md 待办一致）。
- 根因：`error sending request for url` 是 Tauri Rust 网络栈（plugin-http 的 reqwest）在连接阶段失败，不是后端 4xx/CORS/capabilities 问题。最可能是本机对 `skyxing.exe` 进程的网络拦截，或 Rust 网络栈 TLS/DNS 细节；WebView2（浏览器同源）能联网。
- 已做修复：`app/src/lib/api.js` 给 `request()` 增加**回退**——当 `isTauri` 且 plugin-http 报连接层错误时，自动改用 WebView2 原生 `window.fetch`（走系统网络，通常不受进程级拦截），既能自愈也能自诊断；并增强底层 `cause` 错误日志。
- 已启动带修复的重构建（`npm run tauri:build:windows`），但因 Rust 重编译（前端资源变化触发 Tauri 重新嵌入）较慢 + 后台构建与轮询命令在同一 PS 窗口交错，终端输出混乱、用户中断；构建是否完成未确认（21:04 的旧 exe 不含此修复）。**勿用 force-kill 类命令（用户已拒绝）**。
- 后续：①确认带修复的 exe 是否生成；②系统补齐 app 端功能（Turnstile 最优先，因后端登录/注册已要求始终 Turnstile；其次 i18n/主题/动画/跨端同步）。这是独立前端移植大任务，需与用户确认优先级与范围。

### 网络回退修复已生效 + token 同步方案实现
- 用户实测新日志：从 `error sending request for url`（连接失败）变为 `请先完成人机验证`（业务逻辑失败）→ **证明 api.js 的 WebView fetch 回退修复生效，网络已通**，剩余是鉴权层（后端要求登录带 Turnstile token，而 app 无 Turnstile 组件）。
- 用户建议"web 登录后同步登录至 app 端"。已实现：
  - `app/src/context/AuthContext.jsx`：新增 `loginWithToken(token)`（写 token + getMe 验证 + setUser），并暴露。
  - `app/src/pages/LoginPage.jsx`：新增"使用登录令牌登录"区块（粘贴网页版复制的 token 直接登录）。
  - `web/src/pages/MyPage.jsx`：新增"登录令牌 / 复制到桌面客户端可直接登录"卡片 + 复制按钮（读取 `localStorage.skyxing_token` 用 `navigator.clipboard`）。
  - 原理：app 与 web 共用同一 JWT（`localStorage['skyxing_token']` + 同一 JWT_SECRET），app 写入有效 token 即被 AuthProvider 自动认证。
- **本机无法部署 web**：`wrangler deploy` 报 `fetch failed`（Node/wrangler 进程联网被拦，与 Tauri Rust 网络被拦同源；浏览器/WebView 可通）。需用户在能联网的机器执行 `cd e:\SkyXingDev && wrangler deploy --config wrangler.toml`，或临时用浏览器 DevTools `localStorage.getItem('skyxing_token')` 取 token 粘贴到 app。
- app 需重新 `tauri build` 才包含上述修复+令牌登录 UI（之前残留构建不含令牌 UI）。注意先结束残留 cargo/rustc 进程避免文件占用冲突。
- 环境结论：**Tauri Rust 进程与 Node/wrangler 进程在本机无法直连外网，但 WebView2/浏览器可通**。这解释了 app 最初全失败、以及 web 部署失败。

### App 端补齐四项功能（Turnstile / i18n / 主题+动画 / 跨端同步）并重新打包
- 用户指示"这四项都做，做完之后打包"。已将 web 端四套系统移植到 `app/`（Tauri）：
  - `lib/i18n.js`（中英词典，与 web 一致，另加 app.terminal/app.theme/app.debug/app.loginRegister）、`context/I18nContext.jsx`、`context/AnimationContext.jsx`（ANIM_MODES + data-anim）、`context/SyncContext.jsx`（登录拉取+15s 轮询+冲突以后端为准）、`components/TurnstileWidget.jsx` 全部从 web 移植。
  - `lib/api.js`：新增 `getSync/putSync/getSyncVersion/getBotStatus/verifyTurnstile/getConfig/getBootstrap`；`request` 错误携带 `err.data`（识别 `needTurnstile`）。
  - `context/AuthContext.jsx`：`login(username,password,turnstileToken)` 透传 token。
  - `main.jsx`：Provider 嵌套 Settings>I18n>Animation>BrowserRouter>Auth>Sync>Transition。
  - `index.css`：补 `[data-anim="minimal"]`(关动画)/`[data-anim="rich"]`(卡片入场)；`index.html` 内联脚本初始化 `data-anim`（与 data-theme 一致防闪）。
  - `pages/SettingsPage.jsx`：重写——语言(自动/中/英)/动画(minimal/normal/rich)/主题切换 + 同步按钮 + 2FA 模态 + 调试开关 + 注销账号；保留更新/反馈/关于。
  - `pages/LoginPage.jsx`/`RegisterPage.jsx`：接入 i18n + Turnstile（getConfig 拿 siteKey 即渲染，提交带 token，识别 needTurnstile）；保留"登录令牌导入"备用入口（Turnstile CDN 受阻时可用）。
  - `components/Layout.jsx`：导航/设置面板标签 i18n 化。
- 验证：`npm run build`(vite) 通过（仅 plugin-http 动态/静态导入告警，预存无害）；`npm run tauri:build:windows` 成功（Rust release 2m32s，NSIS 打包）。
- 产物：`app/src-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis/SkyXing_2.0.1-beta.1_x64-setup.exe`（3,885,543 字节），已归档 `app/releases/Windows/SkyXing_2.0.1-beta.1_x64-setup.exe`。
- 注意：本次仅重建 Windows 安装包；Android 端（`npm run tauri:build:android`）未构建。web 端若需同步 i18n/同步等改动也需 `wrangler deploy`（本机无法联网部署，须在可联网机器执行）。
<arg_key:6124c78e>explanation</arg_key:6124c78e>
<arg_value:6124c78e>记录网络回退生效、token 同步实现与 web 部署失败原因

### 用户反馈：线上 Web「我的」页无"登录令牌"按钮 + 重新部署 web
- 用户澄清：在 Web 端（skyxing.dpdns.org）「我的」界面看不到"登录令牌 / 复制到桌面客户端"按钮。根因：该按钮只在本地源码 `web/src/pages/MyPage.jsx`（约第103-113行）存在，但 **web 从未 build+deploy**，线上跑的是 `public/` 里的旧 bundle。
- 本机联网状态已恢复：`node fetch('https://skyxing.dpdns.org')` 返回 200（旧"沙箱无外网"结论过时），故可直接部署。
- 重新部署 web：`cd web && npm run build`（vite `outDir:'../public'`、`emptyOutDir:true`）→ 因 emptyOutDir 会清掉 `public/privacy.html`/`terms.html`，先备份再 build、后从备份恢复；产物 `public/index.html` + `assets/index-CvDsFQ5T.js`。
- `wrangler deploy`（根目录）成功：Version `6bae76cb-dc81-422c-a925-4c0989877225`；assets 上传 `index.html` + `index-CvDsFQ5T.js`，绑定 env.SKYXING_KV / env.ASSETS / env.TURNSTILE_SITE_KEY 均正常。
- 现线上「我的」页已含"登录令牌"卡片：点"复制"即把 `localStorage.skyxing_token` 写入剪贴板，提示到桌面客户端「使用登录令牌登录」处粘贴即可登录。
- 更正旧记录：原"本机无法部署 web / 本机无法直连外网"已不成立（2026-07-20 当日网络恢复）。
